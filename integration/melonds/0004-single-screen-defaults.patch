diff --git a/src/frontend/qt_sdl/Config.cpp b/src/frontend/qt_sdl/Config.cpp
index 0d48ee16..f5798708 100644
--- a/src/frontend/qt_sdl/Config.cpp
+++ b/src/frontend/qt_sdl/Config.cpp
@@ -52,6 +52,12 @@ DefaultList<int> DefaultInts =
     {"Instance*.Joystick", -1},
     {"Instance*.Window*.Width", 256},
     {"Instance*.Window*.Height", 384},
+    // psz-melonmix defaults: this build exists to play PSZ on one screen, so a
+    // fresh profile starts there rather than making every user find the
+    // settings. Still user-changeable -- these are defaults, not overrides.
+    // ScreenSizing 4 = TopOnly, ScreenAspectTop 1 = 16:9 (Screen.h aspectRatios).
+    {"Instance*.Window*.ScreenSizing", 4},
+    {"Instance*.Window*.ScreenAspectTop", 1},
     {"Screen.VSyncInterval", 1},
     {"3D.Renderer", renderer3D_Software},
     {"3D.GL.ScaleFactor", 1},
